<h1>All Plots</h1>

<% @plots.each do |plot| %>
  <div id="plot-<%= plot.id %>">
    <p>Plot Number: <%= plot.number %></p>
    <% plot.plants.each do |plant| %>
      <p><%= plant.name %></p>
         <p><%= link_to "Remove Plant", "/plots/#{plot.id}/plants/#{plant.id}", method: :delete, local: true %></p>
        </div>
    <% end %>
<% end %>
